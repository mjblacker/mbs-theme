{% extends "base.twig" %}

{% block content %}
    {% if not function('is_front_page') %}
        <header class="mb-8">
            <h1 class="text-4xl font-bold text-gray-900">{{ post.title }}</h1>
        </header>
    {% endif %}
    
    <div class="prose prose-lg max-w-none prose-headings:text-gray-900 prose-p:text-gray-700 prose-a:text-[#FFF200] hover:prose-a:text-yellow-300">
        {{ post.content }}
    </div>
{% endblock %}
