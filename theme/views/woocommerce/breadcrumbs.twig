{#
    Breadcrumbs Component
    Dynamic breadcrumbs for WooCommerce pages
    Breadcrumb data is provided via global context (breadcrumbs)
#}

<div class="container mt-3 mx-auto px-4 sm:px-6 lg:px-8 py-4">
    <nav class="flex items-center text-sm text-gray-600" aria-label="Breadcrumb">
        {% for crumb in breadcrumbs %}
            {% if not loop.first %}
                <span class="mx-2">/</span>
            {% endif %}
            {% if crumb.current %}
                <span class="text-gray-900 font-medium">{{ crumb.label }}</span>
            {% else %}
                <a href="{{ crumb.url }}" class="hover:text-gray-900 transition-colors">
                    {{ crumb.label }}
                </a>
            {% endif %}
        {% endfor %}
    </nav>
</div>