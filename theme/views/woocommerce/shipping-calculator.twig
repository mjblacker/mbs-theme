{# Custom Shipping Calculator Component
   Props: None (gets current customer data from WooCommerce)
#}

{% set customer = function('WC').customer %}
{% set has_address = customer.get_shipping_postcode() or customer.get_shipping_city() or customer.get_shipping_state() %}

<div class="shipping-address-form-container mt-1">
    {# <button type="button" class="shipping-address-toggle text-blue-600 hover:text-blue-700 text-sm font-medium cursor-pointer underline"
            aria-expanded="false" aria-controls="custom-shipping-form">
        Delivery
    </button> #}

    <a href="#" class="shipping-address-toggle underline" aria-expanded="false" aria-controls="custom-shipping-form" role="button">
        {{ has_address ? 'Change address' : 'Estimate delivery' }}
    </a>

    <div id="custom-shipping-form" class="shipping-address-form hidden mt-4 p-6 bg-white rounded-lg border border-gray-200 shadow-sm">
        <h4 class="text-lg font-medium text-gray-900 mb-4">Calculate Shipping</h4>

        <form class="custom-shipping-calculator space-y-4">
            {# Hidden country field - defaults to Australia #}
            <input type="hidden" name="shipping_country" value="AU">

            <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                <div>
                    <label for="shipping_state" class="block text-sm font-medium text-gray-700 mb-1">State*</label>
                    {% set current_state = function('WC').customer.get_shipping_state() %}
                    {% set au_states = function('WC').countries.get_states('AU') %}

                    <select name="shipping_state" id="shipping_state" class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-black focus:border-transparent" required>
                        <option value="">State</option>
                        {% for state_code, state_name in au_states %}
                            <option value="{{ state_code }}" {{ current_state == state_code ? 'selected' : '' }}>
                                {{ state_name }}
                            </option>
                        {% endfor %}
                    </select>
                </div>

                <div>
                    <label for="shipping_postcode" class="block text-sm font-medium text-gray-700 mb-1">Postcode*</label>
                    {% set current_postcode = function('WC').customer.get_shipping_postcode() %}
                    <input type="text" name="shipping_postcode" id="shipping_postcode"
                           value="{{ current_postcode }}"
                           placeholder="e.g. 3810"
                           class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-black focus:border-transparent"
                           required>
                </div>
            </div>

            <div class="flex justify-end">
                <button type="submit" class="bg-[#FFF200] hover:bg-yellow-500 text-black px-6 py-2 rounded font-medium transition-colors">
                    Update Shipping
                </button>
            </div>
        </form>
    </div>
</div>